# Quickstart

## Prerequisites

- Node.js 20+
- npm
- Sui wallet on testnet

## Install and run

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build:vercel
```

## Required environment

Use `.env` with live Sui testnet IDs and endpoints (`VITE_*` values for frontend).
